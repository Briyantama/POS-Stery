Start admin dashboard (5173) and open logs.
```bash
cd "C:/Users/solui/moon-eye/POS-Stery" && pm2 start ecosystem.config.cjs --only pos-admin-5173 && start wt.exe -d "C:/Users/solui/moon-eye/POS-Stery" pwsh -NoExit -c "pm2 logs pos-admin-5173"
```
