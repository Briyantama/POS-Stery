Start Go API gateway (8000) and open logs.
```bash
cd "C:/Users/solui/moon-eye/POS-Stery" && pm2 start ecosystem.config.cjs --only pos-gateway-8000 && start wt.exe -d "C:/Users/solui/moon-eye/POS-Stery" pwsh -NoExit -c "pm2 logs pos-gateway-8000"
```
