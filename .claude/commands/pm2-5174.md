Start cashier POS (5174) and open logs.
```bash
cd "C:/Users/solui/moon-eye/POS-Stery" && pm2 start ecosystem.config.cjs --only pos-cashier-5174 && start wt.exe -d "C:/Users/solui/moon-eye/POS-Stery" pwsh -NoExit -c "pm2 logs pos-cashier-5174"
```
